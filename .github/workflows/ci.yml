name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Verify Ruby (runner image)
        run: ruby --version

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Check spec files parse
        run: ruby scripts/check-specs

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test

      - name: Build
        run: npm run build

  rust-lockfile-parity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Toolchain pin lives in src-tauri/rust-toolchain.toml (MSRV = lockfile floor).
      - name: Install Rust 1.88.0
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain 1.88.0 --profile minimal
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
          rustc --version

      - uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            src-tauri/target
            starter/src-tauri/target
          # Salt (v2) bust: caches built before the jayson-docs → jayson-editor
          # rename baked the old absolute checkout path into Tauri's build-script
          # permission output, so restoring them breaks the build. The salt keeps
          # restore-keys from pulling any pre-rename cache.
          key: ${{ runner.os }}-cargo-v2-${{ hashFiles('src-tauri/Cargo.lock', 'starter/src-tauri/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-v2-

      - name: Install Linux Tauri dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf

      - name: cargo check --locked (main src-tauri)
        working-directory: src-tauri
        run: cargo check --locked

      - name: cargo check --locked (starter src-tauri)
        working-directory: starter/src-tauri
        run: cargo check --locked

  # T-123q closure of round-3 audit M-2 ("Windows CI gap"). The
  # `#[cfg(windows)]` tests in `src-tauri/src/ipc/fs.rs` exercise the
  # Windows-specific MoveFileExW path that protects against the data-loss
  # bug fixed in T-123o. Without a Windows runner, those tests are
  # functionally synthetic per AGENTS.md §Review playbook convention #4
  # (extended). This job runs the Rust test suite on a real Windows VM so
  # any regression in the Windows save path fails CI before reaching main.
  windows-cargo-test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      # Pin to the same toolchain as rust-lockfile-parity (MSRV floor).
      - name: Install Rust 1.88.0
        run: rustup toolchain install 1.88.0 --profile minimal
        shell: pwsh

      - name: Set default toolchain
        run: rustup default 1.88.0
        shell: pwsh

      - uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            src-tauri/target
          # Salt (v2) bust — see rust-lockfile-parity job for rationale.
          key: ${{ runner.os }}-cargo-v2-${{ hashFiles('src-tauri/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-v2-

      - name: cargo test (Windows fs path)
        working-directory: src-tauri
        run: cargo test --locked
        shell: pwsh
